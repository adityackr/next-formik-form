import React, { Fragment } from 'react';
import SignupForm from '../components/SignupForm';

const HomePage = () => {
    return (
        <Fragment>
            <SignupForm />
        </Fragment>
    );
};

export default HomePage;
